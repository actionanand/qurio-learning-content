# Optional. Defaults to the repository root that contains package.json.
# QURIO_CONTENT_ROOT=/absolute/path/to/qurio-learning-content

# Studio binds to localhost by default for safety.
HOST=127.0.0.1
PORT=4310
